# Command Pipeline Architecture

The root [`COMMAND_PIPELINE.md`](../COMMAND_PIPELINE.md) is the source of truth for the command pipeline architecture.

This file remains only as a compatibility pointer for older links under `docs/`.
