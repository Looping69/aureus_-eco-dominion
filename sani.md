Sani workspace notes

- Purpose: local workspace anchor file required by AGENTS.md.
- Rule: keep `sani-memory.md` updated with concise change history during the session.
- Keep render changes performance-aware; prefer hybrid rounded primitives over globally increasing voxel density.
- Preserve current simulation and placement contracts while improving presentation.
- 2026-05-25: Investigated the weather pipeline and confirmed a split-brain state model: `EnvironmentSystem` rolls `CLEAR/CLOUDY/RAINY/STORM`, `WeatherOverlay` renders `ACID_RAIN/DUST_STORM`, `EnvironmentRenderSystem` expects `CLEAR/RAINY/STORM/TOXIC/HEAT`, and power/water/production still consume stale names directly. Next step is one shared Zimbabwe-credible weather contract wired through sim, render, UI, and gameplay. (|/) Klaasvaakie
- 2026-05-25: Unified the live weather pipeline around one Zimbabwe-credible contract in `engine/weather/weatherModel.ts`: `CLEAR`, `OVERCAST`, `RAIN`, `STORM`, `HEATWAVE`, and `DUST_STORM`. `EnvironmentSystem` now owns climate transitions plus event overrides, old saves are coerced off legacy names, weather events now drive `weatherOverride`, `EnvironmentRenderSystem` consumes full `WeatherState` for fog/lightning/rain/heat/dust lighting, `WeatherOverlay` matches the same states, and power/water/production/agent energy all read shared gameplay modifiers instead of hard-coded stale strings. Verified with `npm run typecheck`, `node --test tests/weather-model.test.ts`, and `npm run build`. (|/) Klaasvaakie
- 2026-05-25: Browser smoke pass on `http://127.0.0.1:4173/` booted the app successfully after the weather refactor; the only console error was the existing Vercel analytics `/_vercel/insights/script.js` 404, not a weather/runtime crash. (|/) Klaasvaakie
- 2026-05-25: Performance pass found the main renderer ownership bug in `game/render/systems/FoliageRenderSystem.ts`: any chunk foliage update rebuilt the whole visible foliage pool and disabled frustum culling globally. Reworked it into chunk-local instanced foliage meshes with shared geometry caching so chunk updates stay local and off-screen foliage can cull again. Also cut avoidable frame churn in `game/render/systems/AgentRenderSystem.ts` by reusing agent target vectors and only swapping state-icon textures when the icon changes, and stopped unconditional runtime telemetry console spam outside localhost in `engine/kernel/Runtime.ts`. Verified with `npm run typecheck` and `npm run build`. Added `tests/foliage-render-system.test.ts` to lock the chunk-local contract, but direct `node --test` still fails on this repo's extensionless browser-ESM import chain, so that new test is structurally right but not yet executable via the current Node runner. (|/) Klaasvaakie
- 2026-05-25: Revived the dead terrain LOD seam. Added shared LOD helpers in `engine/render/utils/TerrainLod.ts`, wired `game/render/systems/TerrainRenderSystem.ts` to assign chunk LOD bands by distance and ignore stale worker results, and taught `engine/jobs/engine.worker.ts` to honor LOD by meshing far terrain and water as larger macro cells with sparser far-foliage sampling instead of always emitting full-detail 1x1 cells. Added `tests/terrain-lod.test.ts` so the LOD band selection is executable under Node, and verified with `npm run typecheck`, `node --test tests/weather-model.test.ts tests/terrain-lod.test.ts`, and `npm run build`. (|/) Klaasvaakie
- 2026-05-25: Started the local preview server for this checkout with `npm run preview -- --host 127.0.0.1 --port 4173` and verified it bound successfully at `http://127.0.0.1:4173/` with no stderr output. (|/) Klaasvaakie
- 2026-05-25: Mapped the Deep Ledger seams before implementation. `game/deepLedgerFoundationPatch.ts` is a prototype monkey-patch on `draw/getState/loadGame`, `index.tsx` still imports both patch files, real engine ownership points already exist in `AureusWorld.toggleViewMode()`, `StateManager`, `ConstructionSystem`, and sim systems, and the shop/HUD still expose stale dungeon-era assumptions. Plan: replace the patch with an engine-owned survey core + sim system, add `SURVEY_DRILL`, migrate old saves, and wire the shop/UI/HUD to the new unlock path. (|/) Klaasvaakie
- 2026-05-25: Replaced the temporary Deep Ledger foundation patch with a real engine-owned implementation. Added a self-contained survey core in `engine/underground/DeepLedgerSurveyCore.ts`, typed wrappers in `engine/underground/UndergroundGenerator.ts`, and `UndergroundSurveySystem` on the sim loop so underground reveal/migration no longer piggybacks on `AureusWorld.draw()`. Added `SURVEY_DRILL` as a real building with render factory + shop wiring + Trust 50 requirement, changed view gating to use `state.underground`/engine rules instead of patch imports, and migrated old saves by backfilling/syncing underground state during revive/load plus preserving legacy mine/headframe access. Removed `game/deepLedgerFoundationPatch.ts`, removed the stale `game/undergroundViewPatch.ts`, and dropped both side-effect imports from `index.tsx`. Verified with `node --test tests/deep-ledger-survey-core.test.ts`, `npm run typecheck`, and `npm run build` (build still emits the existing large-chunk Vite warning only). (|/) Klaasvaakie
- 2026-05-29: Cleared the last pasted Vercel TypeScript blockers. `types.ts` now exports `Action` and `SidebarMode` again for the older UI import surface, and `engine/state/StateManager.ts` no longer repeats `logistics` and `industry` keys inside the initial state object. This addresses the exact `TS2305` and `TS1117` errors from the failing `npm run build` log. (|/) Klaasvaakie
- 2026-05-29: Investigated the bright cyan slivers visible along desert cliffs in the user screenshot and traced them to the water surface underside showing through tiny terrain seams. Updated `engine/render/materials/VoxelMaterials.ts` so the shared water materials render as top-facing surfaces only and stop writing depth like a solid slab, which should remove the blue leak without flattening the terrain or changing shoreline generation. (|/) Klaasvaakie
- 2026-05-31: Continued from the earlier Factorio-style roadmap by strengthening Phase 2's power-grid link instead of jumping ahead to trains. `engine/sim/systems/PowerGridSystem.ts` now separates connected load from stranded demand and tracks industrial demand specifically for wash plants, foundries, refineries, and workshops. `engine/sim/systems/ProductionSystem.ts` now throttles power-hungry industry when a building is not actually connected, `engine/types/game.ts`, `engine/state/StateManager.ts`, and `engine/sim/PersistenceManager.ts` backfill the new power-grid fields, `components/HUD.tsx` now surfaces `Chains` and `Grid`, and `tests/phase2-production-contract.test.ts` pins the new contract. (|/) Klaasvaakie
- 2026-05-31: Continued the roadmap into late Phase 2 and the first Phase 3 logistics slice. `engine/types/game.ts` now defines `AUTOMATION_KITS`, `engine/data/industrialCosts.ts` and `engine/sim/systems/EconomySystem.ts` make higher-tier logistics and power infrastructure spend those kits, `engine/sim/systems/ProductionSystem.ts` turns `GREEN_TECH_LAB` into an automation-kit processor fed by refined materials, alloys, and machine parts, `engine/sim/systems/LogisticsSystem.ts` now treats `TRAIN_STATION` as a real transport node with inter-station rail linking, larger buffers, and faster packet movement, `components/HUD.tsx` surfaces `Kits`, and the Phase 2/Phase 3 contract tests were extended to pin the new production and train-network expectations. (|/) Klaasvaakie
- 2026-05-31: Pushed the next Phase 3 logistics readability pass. `engine/types/game.ts` now records packet transport modes for belt, rail, and drone hops; `engine/sim/systems/LogisticsSystem.ts` gives train stations a short-range drone-served last-mile radius while keeping rail for hub-to-hub movement; `game/render/systems/BuildingRenderSystem.ts` now renders train hubs with clearer beacon and pad language plus distinct rail-vs-drone packet visuals and active regional hub feedback; `engine/data/buildings.ts` now describes rail and station roles more explicitly; and the renderer plus train-logistics contract tests were extended to pin the new handoff. (|/) Klaasvaakie
- 2026-05-31: Extended the Phase 3 logistics layer from readable hubs into named regional sectors. `engine/types/game.ts` now carries train-sector labels plus per-packet sector metadata and aggregate regional/drone metrics, `engine/sim/systems/LogisticsSystem.ts` now assigns deterministic sector names to train stations, tracks inter-sector rail throughput, and applies a light drone-dispatch pressure curve instead of letting last-mile drones stay infinitely cheap, `game/render/systems/BuildingRenderSystem.ts` now renders visible sector labels, bulk inter-sector rail markers, and warmer drone rings as local pressure climbs, and the Phase 3 renderer/logistics contract tests were extended to pin that regional-handoff layer. (|/) Klaasvaakie
- 2026-05-31: Made those sectors economically real instead of purely decorative. `engine/types/game.ts` now carries explicit sector economy and drone charge/upkeep state, `engine/sim/systems/LogisticsSystem.ts` now generates deterministic sector export/import personalities plus recharge-pad capacity and battery upkeep, `engine/sim/systems/EconomySystem.ts` now applies sector export premiums, sector import discounts, and drone upkeep against AGT, `components/HUD.tsx` now surfaces late-game `Rail` throughput and `Charge`, and the Phase 2/Phase 3 contract tests were extended to pin this economy-plus-recharge layer. (|/) Klaasvaakie
- 2026-05-31: Made sector personalities readable in the live HUD and turned drone support into a deliberate late-game build choice. `components/HUD.tsx` now includes a compact `Market` panel that shows the top visible sectors with export/import badges, demand bonuses, recharge-pad totals, and rail flow; `engine/types/buildings.ts`, `engine/data/buildings.ts`, and `engine/data/industrialCosts.ts` add the new `DRONE_DEPOT`; `engine/sim/systems/LogisticsSystem.ts` now treats that depot as a real drone hub with larger local service radius and recharge capacity; and the Phase 2/Phase 3 contract tests were extended to pin both the sector UI exposure and the depot-backed last-mile support path. (|/) Klaasvaakie
- 2026-05-31: Pushed the follow-up readability pass so the Drone Depot is visually distinct in-world and the trade sidebar now carries a full regional market-planning view. `game/render/systems/BuildingRenderSystem.ts` now decorates `DRONE_DEPOT` with its own launch ring, control spire, pad layout, and orbiting drone traffic silhouette, while live logistics overlays also treat depots as active drone hubs. `components/TradeTerminal.tsx` now includes a dedicated `Sector Market` section that lists each visible region’s export focus, import discount, demand bonus, hub count, and flow. The renderer and Phase 3 contract tests were extended to pin both the depot silhouette and the deeper planning view. (|/) Klaasvaakie
- 2026-06-01: Closed the missing integration gap from that depot pass. `engine/data/voxels/buildings/era4/index.ts` now registers `DroneDepotFactory`, `components/SupplySidebar.tsx` now exposes `DRONE_DEPOT` in the icon map, category map, and Era 4 shop list, and the logistics/Phase 2 contract tests now pin the voxel registration plus the shop exposure so the depot is both visible in-world and actually placeable through the normal build flow. (|/) Klaasvaakie
