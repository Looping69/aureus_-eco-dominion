Sani session notes

- Purpose: local workspace anchor file required by AGENTS.md.
- Rule: keep `sani-memory.md` updated with concise change history during the session.
- 2026-05-25: Merged `origin/main` into `feature/deep-ledger-survey-foundation` to clear PR `#49` conflicts, kept the useful new Deep Ledger UI files from main, and resolved the conflict in favor of the engine-owned survey path by removing the resurrected `deepLedgerFoundationPatch` import/file again and wiring `SurveyDrillQuickBuild` to `BuildingType.SURVEY_DRILL` plus real `state.underground` gating. Re-verified with `node --test tests/deep-ledger-survey-core.test.ts`, `npm run typecheck`, and `npm run build`, pushed merge commit `9fc43aa`, and GitHub merged PR `#49` into `main` at `2026-05-25T12:41:54Z` with merge commit `561de5ed7c6c0d7101dce69d1aa556e7dac1ca52`. (|/) Klaasvaakie
- 2026-06-02: Fixed Vercel-facing TypeScript breaks in `engine/render/materials/VoxelMaterials.ts` by adding the missing `sand`, `leafDark`, and `emissiveOrange` materials expected by `engine/data/voxels/BuildingStages.ts`, and corrected `engine/input/InputSystem.ts` to track pointer IDs with a `Set<number>` instead of a `Map<number, PointerEvent>`. Verified with `npx tsc --noEmit` and `npm run build`; both pass now. (|/) Klaasvaakie
