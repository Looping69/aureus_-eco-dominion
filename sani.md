Sani workspace notes

- Purpose: compact running repo note file required by AGENTS.md.
- Keep summaries concise and update after each reply that changes repo state.
- Current focus: late Phase 3 / early Phase 4 stabilization plus build and contract repairs around the engine-owned state layer.
- 2026-06-02: Repaired `engine/state/StateManager.ts` after it drifted onto an older contract. It now uses current chunk bootstrapping, current `commandQueue` flow, current React-facing listener shape, and exposes `serializeState()` for `GameStateManager` again.
- 2026-06-02: Synced back to the live repo and found one more stale save/load seam. `game/world/GameStateManager.ts` had fallen behind the chunked world model by loading `parsed.grid` and broadcasting `SYNC_GRID`; it now loads `parsed.chunks` and broadcasts `SYNC_CHUNKS` to match the current world/worker contract.
- 2026-06-02: This note file had been emptied in the live repo and has now been restored so the running engineering context is visible again.
- 2026-06-03: Continued the approved higher-risk Phase 4 renderer pass on `main`. `game/render/systems/BuildingRenderSystem.ts` now reuses overlay label sprites across frames and moves the remaining transient route-heat, congestion, hub, planner, sector, and junction beacon visuals onto the instanced overlay path instead of recreating one-frame meshes and sprites every update.
- 2026-06-03: Continued the renderer split without forcing the giant `BuildingRenderSystem.ts` body through a risky whole-file replace. The next pure-logic seam now lives in `game/render/systems/LogisticsOverlayTopology.ts`, covering node activity, neighbor lookup, junction detection, drone-hub detection, node world positions, and affected-chunk filtering ahead of the final coordinator switchover.
- 2026-06-04: Continued breaking down the renderer switchover by extracting `game/render/systems/LogisticsOverlayInstancing.ts`, which now owns packet-instance spec creation plus packet and overlay material-cache helpers ahead of the final `BuildingRenderSystem.ts` coordinator swap.
- 2026-06-04: Extracted `game/render/systems/BuildingPreviewController.ts` so ghost building and cursor preview behavior now has its own dedicated module instead of living in the main world-render coordinator.
- 2026-06-04: Extracted `game/render/systems/BuildingEffectEmitter.ts` so transient mining, dust, smoke, and eco rehab particle emission no longer lives inside the main renderer coordinator.
- 2026-06-04: Followed the screenshot-driven next-best renderer path for the remaining bright cyan terrain slashes. `engine/render/materials/VoxelMaterials.ts` now constrains animated water displacement to shallow upward-facing surface vertices, and `tests/water-material-contract.test.ts` pins the water material depth/front-side and low-amplitude surface-wave contract.
- 2026-06-04: Continued into a broader visual-coherence graphics pass. `engine/jobs/engine.worker.ts` now emits a muted biome palette, `engine/render/materials/VoxelMaterials.ts` removes artificial terrain striping while calming water and foliage material response, `engine/render/ThreeRenderAdapter.ts` rebalances exposure and world lighting, and `tests/visual-coherence-contract.test.ts` pins the new non-neon terrain/water/lighting contract.
- 2026-06-04: Addressed the remaining dark terrain tear slashes from the latest screenshot. `engine/jobs/engine.worker.ts` now reserves cliff side-band geometry for true steep drops by raising `CLIFF_FACE_THRESHOLD` to `1.45`, tints the rare remaining cliff faces instead of drawing them as black cuts, and `tests/render-performance-contract.test.ts` pins the tear-reduction contract plus the worker pathfinding call.
- 2026-06-04: Strengthened the terrain tear fix after the latest screenshot still showed diagonal cuts. `engine/jobs/engine.worker.ts` now disables cliff side-band geometry entirely with `Number.POSITIVE_INFINITY`, leaving the smooth top-surface heightfield as the visible terrain mesh, and `tests/render-performance-contract.test.ts` pins that disabled side-band contract.
- 2026-06-04: Followed the latest screenshot to the terrain top-surface triangle shading itself. `engine/jobs/engine.worker.ts` now uses a stable upward terrain surface normal for top faces instead of per-triangle computed normals, removing the dark diagonal lighting split across non-planar tiles, and `tests/render-performance-contract.test.ts` pins the stable-normal contract.
- 2026-06-04: Reversed course on smoothing after the latest screenshot still showed artifacts and restored the sturdier voxel/block terrain style. `engine/jobs/engine.worker.ts` now emits flat block tops plus axis-aligned step faces with the muted palette, and `tests/render-performance-contract.test.ts` pins the restored voxel terrain contract.
- 2026-06-04: Recovered frame rate after the restored voxel terrain emitted too many full-detail faces. `engine/jobs/engine.worker.ts` now uses terrain LOD for voxel surface steps instead of forcing every chunk to 1x1 cells, `game/render/systems/TerrainRenderSystem.ts` tightens the visible chunk ring to 3 mobile / 5 desktop, and `tests/render-performance-contract.test.ts` pins the voxel performance limits.
- 2026-06-05: Fixed the voxel height mismatch that left agents and water visually floating. `engine/jobs/engine.worker.ts` now emits block tops at the shared `height * 0.5` surface convention, keeps pond/reservoir beds one unit lower, lowers zero-height water to sit at the expected surface, and `tests/render-performance-contract.test.ts` pins the alignment.
- 2026-06-05: Tuned world pacing and surface detail. `engine/sim/systems/EventSystem.ts` now checks random events every 180 seconds instead of 30, `engine/sim/logic/AiLogic.ts` lowers weather/economic/geology event odds and makes illegal miners a 0.15% single-camp incursion, `engine/jobs/engine.worker.ts` adds deterministic voxel color breakup for grass/sand/dirt/stone, and contract tests pin the new pacing, rarity, and texture behavior.